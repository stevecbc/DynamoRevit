## In Depth
This node returns the path of an imported instance inside a Revit project.

In this example, a SAT file is imported and used as the input to the ImportInstance.Path node.  The output is the file path location for that imported instance.
___
## Example File

![ImportInstance.Path](./Revit.Elements.ImportInstance.Path_img.jpg)